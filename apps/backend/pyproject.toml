[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra --strict-markers --basetemp=.pytest_temp"

[tool.ruff]
target-version = "py311"
line-length = 100
extend-exclude = [".venv", ".pytest_temp*", ".pytest_tmp*"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "B", "UP", "SIM"]
